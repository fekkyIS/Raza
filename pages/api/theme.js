import { createTheme } from "@mui/material/styles";

// Create a theme instance.
const theme = createTheme({
  typography: {
    fontFamily: 'Proxima Nova Alt Rg'
  },
  palette: {
    spacing: 24,
  },
});


export default theme